# Copy to .env and adjust. EXPO_PUBLIC_ values are built into the app, so
# never put secrets here.

# Backend URL (live Render backend by default).
EXPO_PUBLIC_API_URL=https://beauty-app-g16l.onrender.com

# Local backend alternative (use your PC's LAN IP so a phone can reach it):
# EXPO_PUBLIC_API_URL=http://192.168.1.10:4000
